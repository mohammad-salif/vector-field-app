import { FieldPortalPage } from '@/pages/FieldPortalPage';

/**
 * Logistics Intelligence - Field User App
 *
 * Scoped strictly to the individual field operator (Officer V. Rawat / Unit 4).
 * Government / Admin Dashboard functionality is preserved in separate page modules
 * for Member 1's integration branch.
 */
function App() {
  return <FieldPortalPage />;
}

export default App;
